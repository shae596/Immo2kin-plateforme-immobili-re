[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# JSON sans Blade (évite « Please provide a valid cache path » si storage/views manque).
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
